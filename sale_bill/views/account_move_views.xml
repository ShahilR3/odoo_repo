<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="account_move_view_form" model="ir.ui.view">
        <field name="name">account.view.move.form.inherited</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">
            <xpath expr="//button[@name='button_request_cancel']" position="after">
                <button name="action_create_sale_order_bill" type="object" string="Create SO"
                        invisible="move_type != 'out_invoice' or state !='draft'"/>
            </xpath>
        </field>
    </record>
</odoo>